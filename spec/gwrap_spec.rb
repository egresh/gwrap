# frozen_string_literal: true

RSpec.describe Gwrap do
  it "has a version number" do
    expect(Gwrap::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
